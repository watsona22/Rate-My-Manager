<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Manager Rating - Reviews</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">

    <!-- Main Content -->
    <div class="container mx-auto mt-10 p-6 bg-white rounded-lg shadow-lg text-center">
        <h1 class="text-6xl font-bold mb-6">Manager Reviews</h1>

        <div class="mt-10">
            <h2 class="text-4xl font-bold mb-4">Recent Reviews</h2>
            <div id="ratings-container">
                <script id="rating-template" type="text/x-handlebars-template">
                    {{#if ratings.length}}
                        {{#each ratings as |rating|}}
                            <div class="mb-4">
                                <p class="text-xl font-bold">{{ rating.name }}</p>
                                <p class="text-gray-700">{{ rating.description }}</p>
                                <p class="text-blue-500">Rating: {{ rating.rating }}/5</p>
                            </div>
                        {{/each}}
                    {{else}}
                        <p class="text-xl text-gray-700">No ratings available yet.</p>
                    {{/if}}
                </script>
            </div>
        </div>
    </div>

</body>
</html>
